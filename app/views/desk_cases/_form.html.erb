<%= form_for(@desk_case) do |f| %>
  <% if @desk_case.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@desk_case.errors.count, "error") %> prohibited this desk_case from being saved:</h2>

      <ul>
      <% @desk_case.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
